void setup() {
  pinMode(7, INPUT); // 핀 7을 입력 모드로 설정
  Serial.begin(115200); // 시리얼 통신 시작
}

void loop() {
  if (digitalRead(7)) { // 핀 7에서 값 읽기
    Serial.println("충격감지"); // 충격 감지 시 메시지 출력
    delay(500); // 0.5초 지연
  }
}
